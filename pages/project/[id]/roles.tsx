import SideBarProject from "@components/SideBarProject";
import { Flex } from "@pankod/refine-chakra-ui";

const Roles = () => {
    return (
        <Flex>
            <SideBarProject current="roles" />
        </Flex>
    );
};

export default Roles;
